#include "main.h"
#include <string.h>

/**
 *_strlen - returns the length of a string
 *@s: any char string
 *Return: length of a string
 */

int _strlen(char *s)
{
	int _strlen;

	_strlen = strlen(s);

	return (_strlen);


}
