#include <stdio.h>
/*
 * main - the main function
 * @argc: the argument count.
 * @rgv: the string argument.
 * Return: 0 on succes and 1 on fail
 */
int main(int argc, char *argv[])
{
	printf("%d\n", (argc - 1));

	return (0);
}
