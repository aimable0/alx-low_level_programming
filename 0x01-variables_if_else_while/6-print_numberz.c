#include <stdio.h>
/**
 *main - a program that prints digits using asci codes
 *Return: 0 (success)
 */
int main(void)
{
int num = 48;

	for (num = 48; num <= 57; num++)
		putchar(num);
	putchar('\n');

return (0);

}
