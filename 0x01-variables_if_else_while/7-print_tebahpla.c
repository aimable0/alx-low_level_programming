#include <stdio.h>
/**
 *main - printing alphabets but e and q
 *Return: 0 (success)
 */
int main(void)
{
char al = 'z';

	for (al = 'z'; al >= 'a'; al--)
	{
	putchar(al);
	}
	putchar('\n');

return (0);

}
